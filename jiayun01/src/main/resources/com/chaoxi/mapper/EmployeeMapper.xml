<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chaoxi.mapper.EmployeeMapper">

    <!-- 新增 -->
    <insert id="insert" parameterType="com.chaoxi.pojo.Employee" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO employee (name, age, phone, email, department, salary, status)
        VALUES (#{name}, #{age}, #{phone}, #{email}, #{department}, #{salary}, #{status})
    </insert>

    <!-- 删除 -->
    <delete id="deleteById" parameterType="long">
        DELETE FROM employee WHERE id = #{id}
    </delete>

    <!-- 修改 -->
    <update id="update" parameterType="com.chaoxi.pojo.Employee">
        UPDATE employee
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="age != null">age = #{age},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="email != null">email = #{email},</if>
            <if test="department != null">department = #{department},</if>
            <if test="salary != null">salary = #{salary},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <!-- 根据ID查询 -->
    <select id="selectById" parameterType="long" resultType="com.chaoxi.pojo.Employee">
        SELECT * FROM employee WHERE id = #{id}
    </select>

    <!-- 条件查询 -->
    <select id="selectByPage" resultType="com.chaoxi.pojo.Employee">
        SELECT * FROM employee
        <where>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="department != null and department != ''">
                AND department = #{department}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY create_time DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <!-- 查询总数 -->
    <select id="selectCount" resultType="int">
        SELECT COUNT(*) FROM employee
        <where>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="department != null and department != ''">
                AND department = #{department}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
    </select>

</mapper>